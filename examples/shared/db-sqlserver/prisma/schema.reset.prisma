datasource db {
  provider = "sqlserver"
  schemas  = ["common", "user"]
}

generator client {
  provider        = "prisma-client"
  previewFeatures = ["views"]
  output          = "../src/generated/client/reset"
}

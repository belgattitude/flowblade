name: flowblade-example-nextjs
services:
  app-bun:
    container_name: "flowblade-nextjs-app-bun"
    build:
      # Start from root of the monorepo
      context: ../../../../
      dockerfile: ./examples/apps/nextjs-app/docker/Dockerfile.bun
      tags:
        - flowblade-nextjs-app-bun:latest
    restart: no
    networks:
      - flowblade-net
    ports:
      - 3000:3000

networks:
  flowblade-net:
    driver: bridge
    enable_ipv6: false
